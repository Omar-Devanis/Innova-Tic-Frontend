import React from 'react'

const Sidebar = () => {
    return (
        <nav class="sidebar">
            <button>Perfil</button>
            <button>Proyectos</button>
            <button>Usuarios</button>
        </nav>

    )
}

export {Sidebar};
